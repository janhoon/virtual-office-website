---
---

<div id="features">

  <!-- ── 1 · Voice Channels ── -->
  <section class="feature-section bg-dark">
    <div class="shell">
      <div class="feature-row">
        <div class="feature-content reveal">
          <div class="feature-icon" aria-hidden="true">
            <svg viewBox="0 0 48 48" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
              <rect x="18" y="6" width="12" height="20" rx="6" />
              <path d="M12 22v2a12 12 0 0024 0v-2" />
              <line x1="24" y1="36" x2="24" y2="42" />
              <line x1="18" y1="42" x2="30" y2="42" />
            </svg>
          </div>
          <h2>Voice Channels</h2>
          <p>
            Your team is always one click away. Persistent voice channels mean
            no scheduling, no meeting links. The conversation is already
            open — just join.
          </p>
        </div>
        <div class="feature-media reveal reveal-d2">
          <div class="media-placeholder" data-slot="feature-voice-screenshot">
            <span>[ Feature Screenshot: Voice Channels ]</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ── 2 · Live Presence ── -->
  <section class="feature-section bg-mid">
    <div class="shell">
      <div class="feature-row feature-row-reversed">
        <div class="feature-content reveal">
          <div class="feature-icon" aria-hidden="true">
            <svg viewBox="0 0 48 48" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round">
              <circle cx="24" cy="24" r="5" fill="currentColor" stroke="none" />
              <circle cx="24" cy="24" r="12" opacity="0.5" />
              <circle cx="24" cy="24" r="19" stroke-width="1.6" opacity="0.22" />
            </svg>
          </div>
          <h2>Live Presence</h2>
          <p>
            Know who's available right now. Speke shows who's online, who's
            focused, who's mid-call — without the passive-aggressive green dot.
          </p>
        </div>
        <div class="feature-media reveal reveal-d2">
          <div class="media-placeholder" data-slot="feature-presence-screenshot">
            <span>[ Feature Screenshot: Live Presence ]</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ── 3 · Screen Sharing ── -->
  <section class="feature-section bg-dark">
    <div class="shell">
      <div class="feature-row">
        <div class="feature-content reveal">
          <div class="feature-icon" aria-hidden="true">
            <svg viewBox="0 0 48 48" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
              <rect x="4" y="6" width="40" height="28" rx="3" />
              <line x1="16" y1="38" x2="32" y2="38" />
              <path d="M24 16v10" />
              <path d="M20 20l4-4 4 4" />
            </svg>
          </div>
          <h2>Screen Sharing</h2>
          <p>
            Share what you're seeing, instantly. Multiple people can share
            simultaneously. No host controls, no "can you see my screen?" — it
            just works.
          </p>
        </div>
        <div class="feature-media reveal reveal-d2">
          <div class="media-placeholder" data-slot="feature-screenshare-screenshot">
            <span>[ Feature Screenshot: Screen Sharing ]</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ── 4 · Channels & Threads ── -->
  <section class="feature-section bg-mid">
    <div class="shell">
      <div class="feature-row feature-row-reversed">
        <div class="feature-content reveal">
          <div class="feature-icon" aria-hidden="true">
            <svg viewBox="0 0 48 48" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M6 10a2 2 0 012-2h32a2 2 0 012 2v20a2 2 0 01-2 2H14l-8 6V10z" />
              <path d="M20 14l-2 16M30 14l-2 16" />
              <path d="M15 20h18M14 26h18" />
            </svg>
          </div>
          <h2>Channels &amp; Threads</h2>
          <p>
            Organized channels for every project and topic. Threaded replies
            that don't get lost. A searchable history your whole team can find.
          </p>
        </div>
        <div class="feature-media reveal reveal-d2">
          <div class="media-placeholder" data-slot="feature-channels-screenshot">
            <span>[ Feature Screenshot: Channels & Threads ]</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ── 5 · Desktop App ── -->
  <section class="feature-section bg-dark">
    <div class="shell">
      <div class="feature-row">
        <div class="feature-content reveal">
          <div class="feature-icon" aria-hidden="true">
            <svg viewBox="0 0 48 48" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
              <rect x="6" y="6" width="36" height="26" rx="2.5" />
              <line x1="6" y1="14" x2="42" y2="14" stroke-width="1.4" opacity="0.35" />
              <circle cx="11" cy="10" r="1.4" fill="currentColor" stroke="none" opacity="0.45" />
              <circle cx="16" cy="10" r="1.4" fill="currentColor" stroke="none" opacity="0.45" />
              <circle cx="21" cy="10" r="1.4" fill="currentColor" stroke="none" opacity="0.45" />
              <path d="M2 36h44" />
              <path d="M18 32l-2 4" />
              <path d="M30 32l2 4" />
            </svg>
          </div>
          <h2>Desktop App</h2>
          <p>
            Speke lives in your dock. Native apps for macOS, Windows, and
            Linux. Always running, always ready — not buried in a browser tab.
          </p>
        </div>
        <div class="feature-media reveal reveal-d2">
          <div class="media-placeholder" data-slot="feature-desktop-screenshot">
            <span>[ Feature Screenshot: Desktop App ]</span>
          </div>
        </div>
      </div>
    </div>
  </section>

</div>
